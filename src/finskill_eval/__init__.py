"""finskill-eval: neuro-symbolic evaluation pipeline for financial AI skills.

Architectural rule: the LLM does semantic work; deterministic Python does every
piece of arithmetic and every numerical comparison. The LLM never grades a number.
"""

__version__ = "0.1.0"
