"""M7: validation-gated skill-description optimization.

The verifier scorecard (M2/M5) is the reward signal. We optimize ONLY the skill
description + progressive-disclosure structure; the analytical body is a
protected section, never edited (auto-generated analytical prose underperforms
hand-authored, per the research). Frozen model + trained context.
"""
