"""Option C: LLM-based extraction of an untouched skill's spreadsheet into a
structured ledger. The extraction LLM is part of the GRADER, not the skill —
the skill under test is run and read 100% unmodified. Python still does every
arithmetic recompute and every comparison vs gold (the research-endorsed hybrid:
LLM extraction + rule-based verification).
"""
