"""Ground-truth sources behind one protocol, with frozen point-in-time snapshots.

FMP is the candidate (never gold). SEC XBRL is the independent as-reported
anchor (and bootstrap gold while Daloopa MCP access is pending). Daloopa is the
primary gold for the M6 conversion A/B once available.
"""
